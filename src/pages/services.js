import React from "react";
import Layout from "../components/Layout";

const services = () => {
  return (
    <Layout>
      <div className="full-page">
        <h2>Services</h2>
          <p>If it's broken we can fix it!</p>
          <h3>Air conditioning?</h3>
          <p>No problem we can check for leaks and recharge it!</p>
      </div>
    </Layout>
  );
};

export default services;
